import React from 'react';

export default function HeroSection() {
  return (
    <section className="hero-section">
      <div className="hero-content">
        <div className="hero-tag">Competitive Programming</div>
        <h1 className="hero-title">Never miss a<br /><span className="accent">contest again.</span></h1>
        <p className="hero-sub">Track LeetCode, Codeforces, & CodeChef — all in one place.</p>
      </div>
    </section>
  );
}
